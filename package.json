{
  "name": "websocket-server",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "dev": "nodemon app.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.7.0",    // Add WebSocket dependency
    "nodemon": "^2.0.22"
  }
}
